# Runtime-only dependencies for the packaged desktop app.
# Keep this list lean: dev/test/server-only packages stay in the repo-level requirements.txt.
numpy
torch
transformers
pillow
opencv-python-headless
decord2==2.0.0; sys_platform == "darwin" and platform_machine == "x86_64"
decord2>=3.4.0; sys_platform != "darwin" or platform_machine != "x86_64"
faiss-cpu
imagehash
tqdm
imageio-ffmpeg
